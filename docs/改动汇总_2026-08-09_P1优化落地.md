# 墨韵阁 · 改动汇总（2026-08-09 P1 优化落地）

**日期**: 2026-08-09
**会话**: P1 优化项落地（经典诗词导入 / 分享公开页 / next-image / SEO / TTS 预生成增强）
**Git**: `a4dcd06`（23 files，+581/-23）
**依据**: docs/优化分析_2026-08-09.md 的 P1 功能与性能建议

---

## 一、改动清单

| # | 优化项 | 文件 | 说明 |
|---|--------|------|------|
| 1 | 经典诗词导入 | `src/lib/classic-poems.ts`（新增） | 12 首公版唐诗宋词（李白/杜甫/王维/苏轼等），覆盖四季；`seasonToTerm()` 按季推导默认节气，空季立春兜底 |
| 2 | 经典诗词导入 | `src/components/settings/ClassicImportSection.tsx`（新增） | 标题查重（跳过已存在）、批量导入「四时墨苑」、导入统计卡片与结果展示 |
| 3 | 经典诗词导入 | `src/app/settings/page.tsx` | 设置页接入导入区 |
| 4 | 分享公开页 | `src/app/share/[id]/page.tsx`（新增） | 无鉴权只读展示单诗；`generateMetadata` 输出 OG/Twitter 元信息（封面 dataURL 时降级站点图标）；force-dynamic 实时读 KV |
| 5 | 分享公开页 | `src/app/poem/[id]/page.tsx` | 新增「复制公开链接」按钮（clipboard API，失败降级 window.prompt） |
| 6 | next/image 优化 | `AiPanel.tsx` / `ShareCard.tsx` / `poem/[id]` / `bookmarks` 预览 | 4 处 `<img>` → `next/image`（fill + sizes，自动响应式与懒加载） |
| 7 | 藏主页 SEO | `src/app/yuan/[slug]/layout.tsx`（新增） | 服务端按 slug 查 collection 注入 title/description/OG 元信息 |
| 8 | TTS 预生成增强 | `scripts/tts-pregen.mjs` | 诗词列表优先从 Redis `poems:all` 直读（本地 fetch 走代理易失败），HTTP 作回退 |
| 9 | TTS 预生成增强 | `package.json` / `package-lock.json` | 新增 `undici@^8.10.0`（devDependencies，ProxyAgent 全局接管 Node fetch 走代理） |
| 10 | 静态音频入库 | `public/audio/*.mp3` | 11 首预生成真人音色（约 720KB），随站点分发，TTS 路由静态命中 |

## 二、验证结果

### 构建
- ✅ `next build` 成功，33 路由全部生成，无 TypeScript 错误
- ✅ 新增路由 `/share/[id]`（First Load 111KB）、`/yuan/[slug]`（193KB）

### 生产回归（端口 3001）
| 路由 | 结果 |
|------|------|
| `/` | ✅ 200 |
| `/settings` | ✅ 200 |
| `/yuan/sishi-moyuan` | ✅ 200 |
| `/api/poems` | ✅ 200 |
| `/share/<真实 id>` | ✅ 200（渲染正常） |
| `/share/nonexistent` | ✅ 404（notFound 兜底） |
| `/poem/<真实 id>` | ✅ 200 |

### 踩坑记录
1. **`.next` 产物并发污染**：首次 `next start` 报 `Cannot find module './vendor-chunks/framer-motion.js'` —— 3000 端口用户 dev server 与 build 共用 `.next` 目录，重编译覆盖了构建产物。对策：重建后立即启动测试（缩短窗口）；生产回归改用 3001 端口，不干扰用户 dev server。

## 三、遗留事项

- GitHub 推送 403（用户侧凭据），本地 `a4dcd06` 及之前共 9 个 commit 未推送。
- P1 剩余项未做：诗词日历视图、骨架屏、字体体积优化、API 缓存、无障碍。
- P0 项未排期：AI 配图对象存储持久化、生产部署凭据就绪。
