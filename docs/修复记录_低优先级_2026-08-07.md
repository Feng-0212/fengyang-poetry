# 墨韵阁 · 缺陷修复记录 — 低优先级（Step 4）

**日期**: 2026-08-07
**依据**: docs/修复计划书_2026-08-07.md
**目标**: 修复 2 项代码卫生问题（空壳页面经审查确认保留）

---

## 已完成

### ⑭ 空壳重定向页审查结论 ✅（无需改动）

`/seasons`、`/bookmarks`、`/yuan/sishi-moyuan/trash` 三个 174B 页面经确认是**兼容入口的有意设计**（`redirect()` 到完整版页面），并非冗余或缺陷，保留不改。

### ⑮ AiPanel 嵌套 AnimatePresence ✅

`src/components/ai/AiPanel.tsx` 中外层 `<AnimatePresence>` 直接嵌套内层 `<AnimatePresence>`（标签建议面板），结构不规范。

**修复**：合并为单一 `<AnimatePresence>`，同时管理「标签建议面板」与「赏析草稿面板」的 exit 动画，删除嵌套。

### ⑯ 拼音搜索每键全量匹配无防抖 ✅

搜索页输入每键触发 `search(query)`（Fuse + 逐诗拼音 O(n) 计算），无防抖。

**修复**：`src/app/search/page.tsx` 新增 `debouncedQuery`（250ms 防抖），搜索结果基于防抖值计算；示例/历史点击 `handleSearch` 立即生效；URL 同步、关键词高亮仍跟随即时输入。

---

## 验证

| 项 | 结果 |
|----|------|
| 空壳页面 | ✅ 审查为兼容重定向，保留 |
| AiPanel 结构 | ✅ 单层 AnimatePresence，exit 动画保留 |
| 搜索防抖 | ✅ 250ms，点击快捷词立即搜索 |
| 生产构建 | ✅ 见 Step 5 全量验证 |

## 涉及文件

```
src/components/ai/AiPanel.tsx
src/app/search/page.tsx
docs/修复记录_低优先级_2026-08-07.md（本记录）
```

## 技术细节

- 防抖实现用独立的 `debouncedQuery` state（非覆盖 `query`），使 URL 同步与高亮保持即时，仅计算层防抖，兼顾体验与性能。
- AiPanel 合并后两个面板仍互不干扰（可同时显示），exit 动画由同一 AnimatePresence 统一调度。
