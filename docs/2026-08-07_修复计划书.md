# 墨韵阁 · 缺陷修复计划书

**日期**: 2026-08-07
**依据**: 全项目代码审查（2026-08-07），详见 docs/ 下既有交付记录与本计划书
**目标**: 修复代码审查中确认的全部缺陷，每一步完成后写交付记录并提交 Git（格式同既有交付记录）

---

## 背景

2026-08-07 对项目完成全面审查（33 个页面/路由、9 个 lib、12 个组件、部署配置），确认生产构建通过（29 路由），但存在 5 项高优先级功能缺陷、8 项中优先级健壮性问题、3 项低优先级代码卫生问题。

## 修复范围

### 🔴 Step 2：高优先级（明确功能缺陷，5 项）

| # | 缺陷 | 方案 | 涉及文件 | 验证 |
|---|------|------|----------|------|
| 1 | 节气表数据错误：10月1-22日错显示「立春」（8月有垃圾占位记录） | 重写 SOLAR_TERM_BY_DATE 为全年连续区间，程序化验证 365 天无空洞 | `src/lib/solarterms.ts` | Node 脚本验证全年每天节气正确 |
| 2 | 写作热力图永远为空（createdAt 为 number 而非 string）+ UTC 日期偏移 | 改用 Date 对象本地时区生成日期键，兼容旧 string 数据 | `src/app/stats/page.tsx` | Node 模拟验证日期键生成 |
| 3 | AI 配图回退图池文件缺失（public/images/ 不存在，全 404） | 生成 14 张水墨风 SVG 回退图入库，更新路由路径为 .svg | `public/images/fallback/*.svg` + `src/app/api/ai/image/route.ts` | ls 验证文件存在 + 构建 |
| 4 | 画廊布局日期显示「undefined/undefined」 | 改为 Date 对象取 月/日 | `src/app/yuan/[slug]/page.tsx` | Node 验证输出 |
| 5 | 写诗/改诗页连续弹两次密码框 | 移除按钮外层多余的 requirePassword（内部已含） | `src/app/yuan/[slug]/write/page.tsx`、`src/app/poem/[id]/edit/page.tsx` | 构建 + 代码走查 |

### 🟡 Step 3：中优先级（健壮性 / 一致性 / 安全，8 项）

| # | 缺陷 | 方案 | 涉及文件 | 验证 |
|---|------|------|----------|------|
| 6 | 生产依赖混入 @vegamo/deepcode-cli（未提交） | 从 dependencies 移除并更新 lock | `package.json` | npm install 后 diff 干净 |
| 7 | 语义搜索路由 Redis.fromEnv() 无环境变量即崩溃 | 改用 lib/kv 的 getKv（含内存回退） | `src/app/api/ai/search/route.ts` | 本地无 Redis 请求不 500 |
| 8 | 双数据源不同步：seasons 页读 IndexedDB 本地而非云端 | seasons 页改走云端 API；删除 db.ts 死代码（exportAllData/importData/getPoemsBySolarTerm/getPoemsBySeason）；清理 settings 页死 import | `src/app/yuan/sishi-moyuan/seasons/page.tsx`、`src/lib/db.ts`、`src/app/settings/page.tsx` | grep 确认无引用 + 构建 |
| 9 | 收藏计数语义混乱（favoriteCount 被当多人收藏） | tags 页热门榜改为个人收藏榜（按 isFavorite）；Poem 类型注释澄清 | `src/app/tags/page.tsx`、`src/types/poem.ts` | 构建 |
| 10 | 回收站 30 天自动清理未实现 | GET /api/poems 惰性清理（每天最多一次，Redis 记时） | `src/app/api/poems/route.ts`、`src/app/trash/page.tsx`（文案） | 单测逻辑 + 构建 |
| 11 | AI 标签池三处不一致（18/24/12） | 新建 lib/tags.ts 统一 24 标签池；TagInput 用前 12 个 | 新建 `src/lib/tags.ts`、`src/app/api/ai/tags/route.ts`、`src/app/api/poems/batch-tags/route.ts`、`src/components/poem/TagInput.tsx` | grep 确认三处同源 |
| 12 | 语义搜索结果被丢弃（仅给文本结果加权） | useSearch.search() 将语义命中但文本未命中的诗并入结果 | `src/hooks/useSearch.ts` | 逻辑走查 |
| 13 | 密码安全：fallback 硬编码进 bundle、服务端认 NEXT_PUBLIC 密码 | 前端移除硬编码密码（仅收集输入）；服务端只认 POEM_PASSWORD；401 时弹窗提示重输 | `src/components/auth/PasswordGate.tsx`、`src/lib/auth.ts`、`src/lib/api.ts` | 构建 + 走查（⚠️ 生产需配置 POEM_PASSWORD，见交付记录） |

### 🟢 Step 4：低优先级（代码卫生，3 项）

| # | 项 | 方案 | 涉及文件 |
|---|----|------|----------|
| 14 | 空壳重定向页（/seasons、/bookmarks、/yuan/sishi-moyuan/trash） | **审查结论：为兼容入口的有意设计，保留不改** | — |
| 15 | AiPanel 嵌套 AnimatePresence | 拆分为独立结构 | `src/components/ai/AiPanel.tsx` |
| 16 | 拼音搜索每键全量匹配无防抖 | 搜索输入加 250ms 防抖 | `src/app/search/page.tsx` |

### ⛔ 已知限制（本轮不修，文档记录原因）

| 项 | 原因 |
|----|------|
| AI 配图 URL 过期（coverImage 存第三方 URL） | 需要引入对象存储/图床基础设施，超出代码修复范围 |
| TTS 生产回退浏览器音色（Vercel 数据中心 IP 被微软封禁） | 平台限制；预生成需本地代理手动执行，无 CI 自动化条件 |
| 备份为同库快照（非异地容灾） | 需 GitHub PAT / 对象存储密钥等外部凭据 |
| First Load JS 偏大（/poem/[id] 235KB、/settings 236KB） | 涉及 framer-motion/fuse.js 等依赖级优化，收益与风险不成比例，留作后续专项 |
| 每次写操作后清除本地密码（需反复输入） | 有意的安全设计（commit 6a9ca5e），保留 |

## 交付流程

1. 每个 Step 完成后：更新本计划书状态 → 写对应交付记录到 `docs/` → `git add` 该 Step 涉及文件 → `git commit`
2. Step 5：全量 `next build` + 关键路径回归 → 写修复总结
3. 交付记录格式与既有文档一致（目标/内容/验证/技术细节/commit hash）

## 风险与注意事项

- **#13 安全修复是行为变更**：生产环境若当前只配置了 `NEXT_PUBLIC_POEM_PASSWORD`，上线后写操作将全部 401。**必须在部署前配置 `POEM_PASSWORD` 环境变量**。
- **#8 删除 db.ts 函数**：删除前已确认无引用；seasons 页改走云端 API 后保持一致数据源。
- **#1 节气表重写**：以既有 SOLAR_TERMS_META 的节气起始日为准，重写后全年 365 天均有映射。
