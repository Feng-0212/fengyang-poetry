# 墨韵阁 · 改动汇总（2026-08-09 遗留风险收尾）

**日期**: 2026-08-09
**会话**: 修复全部遗留风险（①~⑤）+ 状态审查收尾（⑥~⑨）
**Git**: main（待提交：今日 5 项修复 + 本次收尾修复）

---

## 一、本会话全部改动清单

### A. 遗留风险修复（①~⑤，此前已完成，本次一并审查提交）

| # | 风险 | 改动 |
|---|------|------|
| ① | 部署前必须配置非公开 POEM_PASSWORD | `src/lib/auth.ts`：生产环境未配置密码时写操作返回 503（not-configured），不再静默放行 |
| ② | AI 配图 URL 过期裂图 | `src/app/api/ai/image/route.ts`：生成后下载图片转 dataURL 自包含存储（>4MB 退回原 URL） |
| ③ | TTS 生产回退浏览器音色 | `scripts/tts-pregen.mjs` 输出静态 mp3 到 `public/audio/`；`src/app/api/ai/tts/route.ts` 缓存未命中时优先读静态文件 |
| ④ | 备份为同库快照（非异地容灾） | `src/app/api/backup/route.ts`：新增 `BACKUP_REMOTE_URL`/`BACKUP_REMOTE_TOKEN` 异地推送（失败不阻断）；`.env.example` 同步 |
| ⑤ | First Load JS 偏大 | `src/components/layout/ClientShell.tsx`：SearchModal 动态导入（fuse.js 分包）；`src/app/layout.tsx`：LazyMotion 注入动画特性；全站组件 `motion` → `m` 别名 |

### B. 状态审查收尾修复（⑥~⑨，本次新增）

| # | 问题 | 改动 |
|---|------|------|
| ⑥ | 30 字节测试音频残留 `public/audio/c873a14603f34973cebdebe3.mp3` | 已删除（会被 TTS 当真实音频返回） |
| ⑦ | tts-pregen 对已缓存诗跳过 → 静态 mp3 永不生成 | `scripts/tts-pregen.mjs` 重写循环为三态：齐全→跳过；静态在缓存缺→回填；静态缺→合成并双写 |
| ⑧ | layout.tsx 注释夸大 LazyMotion 收益 | 修正为如实描述（v11.18.2 无法真分包，收益来自 SearchModal 动态导入） |
| ⑨ | 会话改动未提交、无留档 | 本文件 + 对话记录续记 |

---

## 二、验证结果

### ① 生产构建
- ✅ Compiled successfully
- ✅ Linting and checking validity of types 通过
- ✅ 32 路由全部生成，无 TypeScript 错误

### ② 运行时回归（`next start` 生产模式实测）
| 路由 | 结果 | 说明 |
|------|------|------|
| `/` | ✅ 200 | 首页正常 |
| `/api/poems` | ✅ 200 | 数据正常 |
| `/api/backup/history` | ✅ 200 | 无 Redis 环境不再 500 |
| `/yuan/sishi-moyuan` | ✅ 200 | 数据源云端 API 正常 |
| `/stats` | ✅ 200 | 热力图页面正常 |

### ③ 鉴权（修复 ① 生效验证）
| 场景 | 结果 |
|------|------|
| 生产环境未配置 POEM_PASSWORD + 写操作 | ✅ 503 not-configured（拒绝，不再静默放行） |
| backup/history 未配置 CRON_SECRET | ✅ 放行（设计如此：未配置不拦截） |

### ④ TTS 静态文件逻辑（修复 ③ 验证）
- ✅ Node fetch 构造 hash 命中 `public/audio/<hash>.mp3` → 200 + `X-Cache: STATIC`
- ✅ 文件不存在 → 503 + `X-Cache: MISS`（快速降级浏览器 TTS）
- ✅ 测试残留文件已删除，不再有假音频被返回

### ⑤ 体积（修复 ⑤ 验证）
| 路由 | 修复前 | 修复后 |
|------|--------|--------|
| `/poem/[id]` | 235 KB | **196 KB** |
| `/settings` | 236 KB | **197 KB** |

收益主要来自 SearchModal 动态导入（fuse.js 分包）。

---

## 三、部署注意（再次强调）

1. **Vercel 必须配置非公开 `POEM_PASSWORD`**，否则生产写操作全部 503。
2. 异地容灾 `BACKUP_REMOTE_URL`/`BACKUP_REMOTE_TOKEN` 为可选，未配置维持同库快照。
3. 如需真人音色离线分发：本地 `node scripts/tts-pregen.mjs` 生成 `public/audio/*.mp3` 后随站点提交部署。
4. 本地 `.env` 当前为空模板，本地 `npm run dev`（development 模式）不受 503 限制，可正常联调。
