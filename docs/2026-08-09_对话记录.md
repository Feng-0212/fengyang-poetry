# 墨韵阁 · 对话记录（2026-08-09 遗留风险修复）

**日期**: 2026-08-09
**会话主题**: 修复全部遗留风险（2026-08-07_修复总结 中列出的 5 项已知限制）
**约定**: 自本对话起，每一次改动均留档于 `docs/`，含对话记录与改动汇总。

---

## 会话记录

### 开场（20:03）
- 用户要求：① 从现在开始每一次改动都要留档（存在 docs/，包括对话记录和改动汇总）；② 把遗留风险全部修了。
- 遗留风险清单（源自 docs/2026-08-07_修复总结 与 docs/2026-08-07_修复计划书）：

| # | 风险 | 原定原因 |
|---|------|----------|
| ① | 部署前必须配置非公开 `POEM_PASSWORD`，否则写操作 401 或形同虚设 | 部署配置，非代码缺陷 |
| ② | AI 配图 URL 过期（coverImage 存第三方临时 URL） | 需对象存储/图床基础设施 |
| ③ | TTS 生产回退浏览器音色（Vercel IP 被微软封禁） | 平台限制，预生成需本地手动执行 |
| ④ | 备份为同库快照（非异地容灾） | 需 GitHub PAT / 对象存储密钥等外部凭据 |
| ⑤ | First Load JS 偏大（/poem/[id] 235KB、/settings 236KB） | 依赖级优化，收益与风险不成比例 |

### 研读（20:03-20:05）
- 通读：auth.ts（服务端密码校验）、api/ai/image/route.ts（配图两步生成+回退图池）、api/ai/tts/route.ts（TTS 缓存+快速失败）、api/backup/route.ts（同库快照）、lib/ai.ts（客户端 AI 封装）、TtsButton.tsx（前端降级）、app/layout.tsx（根布局）。

### 计划
1. 建立留档机制（本文件 + 改动汇总）
2. 逐项修复 ①~⑤
3. 构建验证 + 回归测试 + 汇总留档

---

## 会话续记（20:54-21:00 · 状态审查与收尾修复）

### 背景
上一段会话已完成 ①~⑤ 的代码改动（工作区未提交），本次开头先对网站做整体状态审查，确认是否还有遗留问题。

### 审查发现
| # | 问题 | 严重度 |
|---|------|--------|
| ⑥ | `public/audio/c873a14603f34973cebdebe3.mp3` 为 30 字节测试残留（ID3 标签 "test-audio"），会被 TTS 路由当真实音频返回 | 高 |
| ⑦ | `scripts/tts-pregen.mjs` 对已缓存诗直接 `continue`，静态 mp3 永不生成，Redis 清空后无法兜底 | 中 |
| ⑧ | layout.tsx 注释声称 LazyMotion"显著减小共享 JS"，经源码核查 v11.18.2 无法真分包（`framer-motion/m` 子路径不导出 `m`/`AnimatePresence`；`layoutId` 依赖 `domMax`），实际收益来自 SearchModal 动态导入 | 低（注释失真） |
| ⑨ | 本会话改动未提交、无改动汇总留档 | 低（流程） |

### 处理
1. 删除 ⑥ 测试音频文件（已实测 TTS 会返回它，属确认缺陷）
2. 修复 ⑦：重写循环为三态分支——静态+缓存齐全→跳过；静态在缓存缺→回填 Redis；静态缺→合成并双写
3. 修正 ⑧ 注释为如实描述（保留 LazyMotion + `m` 正确架构）
4. 补本对话记录 + 新建改动汇总文档（见 docs/2026-08-09_改动汇总_遗留风险收尾）
5. 全量构建 + 运行时回归 + 提交推送

### 验证
- `next build`：32 路由全部生成，无 TS 错误
- 运行时：`/`、`/api/poems`、`/api/backup/history`、`/yuan/sishi-moyuan`、`/stats` 全部 200
- auth：生产无 POEM_PASSWORD → 写操作 503 not-configured（保护生效）
- TTS 静态命中：Node fetch 构造 hash → X-Cache: STATIC 返回文件（删除测试文件后降级 503 正常）
- First Load JS：`/poem/[id]` 235→196KB、`/settings` 236→197KB（SearchModal 动态导入收益）

---

## 会话续记（21:15-21:36 · 本地环境配置与凭据打通）

### 背景
上段会话完成代码修复并提交（`e3c8506`、`d63f306`），但推送到 GitHub 被拒（403，凭据账号 `wqx-txdsyl` 无仓库写权限，待用户侧处理）。随后进入本地使用阶段：配置本地 `.env` 环境变量。

### 过程
1. 用户要求"先本地使用"，检查 `.env` 发现全部变量为空（仅模板）。
2. 用户提供 Upstash Redis 与 Agnes AI 凭据，逐项验证：
   - **AI 文本模型** `agnes-2.0-flash` → ✅ 200
   - **AI 图像模型** `agnes-image-2.1-flash` → ✅ 200（返回真实图片 URL）
   - **Upstash Redis** 首次 token（62 字符，解码含非法字符 `H`）→ ❌ WRONGPASS；对用户追问"AI_API_KEY 是干啥的"做了概念澄清（模型名 = 点菜，API Key = 会员卡）
   - 用户重新提供 AI Key（`sk-tAli...`）→ ✅ 验证通过，已填入
   - 用户第三次提供 Redis token（解码为纯 hex 结构 `p261e28e5e9841402e861e9b192b03aa5f`）→ ✅ PONG，`poems:all` 读出云端真实数据
3. 端到端验证：`/api/ai/annotate` 生成《静夜思》完整赏析 200；`/api/poems` 返回云端诗词；`/yuan/sishi-moyuan` 200。

### 结论
- `.env` 最终状态：AI 四件套 ✅、Redis ✅；`POEM_PASSWORD`/`CRON_SECRET`/`NEXT_PUBLIC_SITE_URL` 为空（本地无需，生产部署时再配）。
- 数据持久化恢复：写入的诗词存 Upstash Redis，重启 dev 不丢失，与生产同源。
- 遗留：GitHub 推送 403（用户侧凭据）；TTS 静态预生成未跑（可后续执行 `node scripts/tts-pregen.mjs`）。

---

## 会话续记（21:40-21:50 · 文档补全：留档 + 优化分析 + PRD）

### 任务
用户要求：① 写改动文档（对话内容 + 修复改动）；② 寻找网站进一步优化方向；③ 补写缺失的 PRD。

### 产出（docs/ 新增 3 份文档）
1. **2026-08-09_改动汇总_本地环境配置**：本次环境配置会话的完整记录（配置内容/验证/踩坑/遗留）。
2. **2026-08-09_优化分析**：现状能力盘点 + 20 条优化建议（P0 数据安全 4 条 / P1 功能 6 条 / P1 性能体验 6 条 / P2 远期 4 条）+ 不建议做的事项（避免过度工程）。
3. **2026-08-09_PRD_墨韵阁**：完整产品需求文档（定位/目标/架构/25 条功能需求/非功能需求/数据模型/里程碑/验收标准）。

### 关键优化方向（摘要）
- **P0**：AI 配图对象存储持久化、生产部署凭据就绪（Vercel + GitHub 推送 403）、TTS 预生成落地、收藏语义澄清。
- **P1**：经典诗词导入、诗词日历视图、分享公开页、next/image 图片优化、字体体积、API 缓存、骨架屏、无障碍。
- **P2**（需决策）：多用户/登录、AI 续写仿写、内容社区化、App 打包。
- **不做**：LazyMotion 深层分包（v11.18.2 限制）、自研搜索、数据库迁移。

### 遗留
- GitHub 推送 403 未解决（用户侧凭据）；PRD/优化分析中的 P0 项待排期执行。

---

## 会话续记（22:00-23:00 · P1 优化项落地：经典导入/分享公开页/图片优化）

### 背景
承接上文，用户指示"继续做吧，别忘了写文档留痕"。工作区此时已有一批未提交的 P1 优化改动（上一会话尾声开始做的），本次先审查、再验证、后提交，并补留档。

### 工作区待提交内容（本次审查对象）
| 优化项（对应2026-08-09_优化分析 的 P1） | 改动 |
|------|------|
| 经典诗词导入 | `src/lib/classic-poems.ts`（12 首公版唐诗宋词）+ `src/components/settings/ClassicImportSection.tsx`（查重/批量导入/统计），settings 页已接入 |
| 分享公开页 | `src/app/share/[id]/page.tsx`（无鉴权只读 + OG/Twitter 元信息，封面 dataURL 已兼容）；poem 详情页新增"复制公开链接"按钮 |
| next/image 图片优化 | AiPanel / ShareCard / poem 详情 / 壁纸预览 4 处 `<img>` → `next/image`（next.config 已配 https 通配 remotePatterns） |
| 藏主页 SEO | `src/app/yuan/[slug]/layout.tsx` 服务端注入 collection 元信息 |
| TTS 预生成增强 | `scripts/tts-pregen.mjs` 优先从 Redis 直读诗词列表（避免本地 fetch 走代理失败），HTTP 作回退；`package.json` 新增 `undici@^8.10.0`（ProxyAgent 全局接管，devDependencies，要求 node>=22.19 本地 v24 ✅） |
| 静态音频入库 | `public/audio/*.mp3` 11 首（约 720KB）随站点分发 |

### 审查结论
- 4 个新文件全部质量过关；`AiPanel.tsx` diff 中一处疑似乱码经 grep 确认是终端显示问题（源文件为 `《{poem.title}》· AI 配图`）。
- `ClassicImportSection` 空季诗兜底 season=spring + solarTerm=lichun（立春），语义自洽。
- `.gitignore` 未排除 `public/audio/`，可提交。

### 验证
1. `next build`：33 路由全部生成，无 TS 错误；新增 `/share/[id]`（111KB）、`/yuan/[slug]` 元信息生效。
2. 生产回归（端口 3001，避开用户 3000 dev server）：
   - `/`、`/settings`、`/yuan/sishi-moyuan`、`/api/poems` 均 200
   - `/share/<真实 id>` 200（渲染正常）、`/share/nonexistent` 404（兜底正常）
   - `/poem/<真实 id>` 200
3. **踩坑**：首次 `next start` 500 —— `.next` 产物被 3000 端口 dev server 重编译污染（`vendor-chunks/framer-motion.js` 缺失）。对策：重建后立即启动测试；不改动用户 dev server。
4. 提交 `a4dcd06`（23 files，+581/-23）。

### 遗留
- GitHub 推送 403（用户侧凭据）仍未解决。
- P1 剩余项未做：诗词日历视图、骨架屏、字体体积、API 缓存、无障碍。

---

## 会话续记（23:00-23:20 · P1 剩余优化项全部落地）

### 任务
用户指示"全部改完一起推"——继续完成优化分析中 P1 剩余全部项，统一构建验证后一起推送。

### 实施（对应2026-08-09_优化分析 的 P1 编号）
| # | 项 | 实现 |
|---|-----|------|
| 6 | 诗词日历视图 | 新增 `/yuan/sishi-moyuan/calendar`：月历网格（上/下月切换、今日高亮、当日诗数徽标、节气切换标记），点选日期查看当日诗词；入口在四时墨苑 Hero |
| 7 | AI 朗读延续 | 新增 `TtsSequenceButton`（列表连续朗读）：顺序调用 /api/ai/tts + Audio 播放，播完自动下一首，失败跳过，进度显示 x/y，与 TtsButton 共用音色偏好；接入四时墨苑筛选栏 |
| 9 | 数据统计增强 | stats 页新增：收藏 Top10（前三名高亮）、最长连续写作天数、节气写作分布（Top8 条形图，按节气色） |
| 10 | 批量操作 | tags 页新增"批量打标签"：全库多选 → 输入标签（逗号分隔）→ 合并去重逐首 updatePoem（密码保护）；回收站批量恢复/删除此前已具备 |
| 12 | 字体体积 | Google Fonts 改 `media="print"` onload 非阻塞加载（display=swap 已有）；字重经核查 300-700 全部在用，无缩减空间；顺带修复 share 页未定义变量 `--font-noto` → `--font-lxgw` |
| 13 | API 响应缓存 | `lib/api.ts`：getAllPoems 短 TTL(10s) 内存缓存 + 并发请求去重（四时墨苑页 5 处 hook 由 5 次请求合并为 1 次）；全部写操作成功后失效 |
| 14 | 路由级分包 | 低频页链接 `prefetch={false}`（Navbar 接龙、四时墨苑日历入口）；大依赖已无（fuse.js 已分包，chain 页为本地逻辑） |
| 15 | 骨架屏与空状态 | tags 页加载态由"加载中..."改为骨架网格；stats/四时墨苑/诗详情此前已具备 |
| 16 | 无障碍 | SearchModal 补 `role="dialog"`/`aria-modal`/`aria-label`/遮罩 `aria-hidden`，输入框 aria-label；Navbar ⌘K 按钮 aria-label |

### 踩坑记录
1. **RSC 序列化**：`<link onLoad={fn}>` 触发 "Event handlers cannot be passed to Client Component props"（/trash prerender 失败）。改 `media="print" onload="this.media='all'"` 字符串属性方案解决。
2. **TS 类型**：stats 页 `getSolarTermMeta(key)` 需 `key as SolarTermKey` 断言。

### 验证
- `next build`：30 路由全部生成，无 TS 错误；新路由 `/yuan/sishi-moyuan/calendar`（191KB）。
- 生产回归（3001 端口）：`/`、`/yuan/sishi-moyuan`、`/yuan/sishi-moyuan/calendar`、`/stats`、`/tags`、`/trash` 全部 200；`/share/nonexistent` 404。
- 提交 `c9574e0`（10 files，+739/-15）。

### 遗留
- GitHub 推送（上轮网络重置）未再试；本地 commit：`a4dcd06`、`44de005`、`c9574e0` 共 3 个待推。
- P0 项仍未排期：AI 配图对象存储持久化、生产部署凭据就绪、收藏语义澄清。
- 优化分析"不建议做"清单（LazyMotion 分包/自研搜索/数据库迁移）保持不变。

---

## 会话续记（23:20-23:30 · "能修的全修了"——P0 收尾）

### 任务
用户重申"我说了能修的全修了"。梳理剩余可修项（P0 清单）并逐一处理。

### 处理
| P0 项 | 处理 | 结果 |
|-------|------|------|
| 1 AI 配图持久化 | **根治**：`route.ts` 下载图片后一律 sharp 压缩为 WebP（1024 上限、q85，约 100-300KB）存 dataURL；下载超时 15s→45s。实测 1.4MB 原图 → 207KB WebP dataURL，远低于 Upstash 单 key 1MB 限制 | ✅ 不再裂图、不再撑 Redis |
| 2 生产部署凭据 | 生成 `docs/2026-08-09_部署清单`（Vercel 环境变量表/Cron/验收清单）；录入需用户操作 | 📋 文档就绪 |
| 3 TTS 静态音频 | 重跑 tts-pregen（source .env 后）：14 条记录全部"静态+缓存齐全"，已全覆盖 | ✅ 无需新增 |
| 4 收藏语义澄清 | `api.ts` toggleFavorite 加语义注释（单用户下 favoriteCount 为冗余 0/1，仅排序用；多用户需重构关系表） | ✅ 澄清 |

### 关键发现（AI 配图裂图根因）
- 实测 agnes 生图 URL：**下载需约 20 秒**，原 15s 超时必然失败 → 一直退回原 URL（裂图隐患）。
- 原 1.4MB 图转 dataURL 后约 1.87MB，**超 Upstash 免费版单 key 1MB 限制**，即使转换成功存 Redis 也会失败。
- 验证时发现：刚生成的 agnes URL 约 20 分钟内即过期——裂图问题真实存在。
- 修复后实测全链路：生图 → 下载（45s 超时内）→ sharp WebP → 207KB dataURL（✅）。

### 验证
- `next build` 通过（sharp import 编译正常）。
- 生产回归：`/`、`/api/poems`、`/stats`、`/yuan/sishi-moyuan` 200；`/api/ai/image` 实测返回 WebP dataURL（fallback:false）。
- 提交 `f293f11`（3 files，+73/-11）。
- 说明：验证过程真实调用了 2 次 AI 生图（消耗少量额度）。

### 遗留（非代码可修）
- Vercel 环境变量录入（需用户操作，清单已给）。
- 异地备份端点（需外部服务）。
- P2 方向（多用户/AI 续写/社区化/App）待用户决策。

---

## 会话续记（23:40-00:10 · 多用户公开化改造落地）

### 决策（用户确认）
1. 定位 **公开书院**（非私人园林）。
2. 登录方式：**邮箱+密码**（自建，零外部依赖）。
3. 可见性：**默认公开，可切换私密**（私密仅本人可见）。
4. 旧数据归属：**ADMIN_EMAIL 预留管理员**，该邮箱注册时自动继承全部历史数据。

### 实施（详见 docs/2026-08-09_改动汇总_多用户公开化）
- 服务端：`lib/user.ts`（scrypt 哈希 / Bearer 会话 30 天 / requireUser / 可见性 / claimLegacy）+ 4 个 auth API；全部业务 API 改为 token 鉴权 + ownerId 归属 + visibility 过滤（读过滤、写鉴权、防篡改归属字段）。
- 前端：PasswordGate 演进为登录态 Provider（原接口兼容，10 处使用零改动）；新增 /login 页；Navbar 用户区；写诗/编辑可见性切换；详情/卡片显示 @作者 与私密标识；api.ts 客户端自动 Bearer + 401 清会话。

### 验证（生产模式实测 10 项全过）
注册✅ / 未登录读公开✅ / 建公开+私密诗✅ / 未登录列表无私密✅ / 未登录读私密 404✅ / 未登录写 401✅ / 他人读私密 404✅ / me✅ / 重复注册 409✅ / 页面全 200✅。测试数据已清理。

### 提交
`90a972e`（23 files，+924/-221）。

### 遗留
- 收藏计数重构（他人收藏需 #19 社区化时做 poem_id→user_id 关系）。
- `/yuan/[slug]/write` 通用写诗页可见性开关（默认公开兜底）。
- #18 AI 续写、#19 社区化、#20 App 仍未决策。

---

## 会话续记（00:10-00:30 · #18 AI 续写仿写 + #19 社区化落地）

### 任务
用户指示"vercel 先不管，把 18、19 先做了"——推进 P2 的 AI 深度集成与内容社区化。

### 实施（详见 docs/2026-08-09_改动汇总_AI续写与社区化）
- **#18**：新增 `/api/ai/write`（continue 续写 / parody 仿写，复用文本管线+限流）；写诗页加「AI 续写」「AI 仿写」按钮。
- **#19**：真实收藏（Poem.favoritedBy 列表 + favorite toggle API + 详情页真实语义 + tags 全站收藏榜）；节气同题创作（seasons 页入口 + 写诗页 ?solarTerm 预设）；用户主页 `/u/[userId]`（公开作品聚合，@作者 可点击）。

### 验证（生产模式实测）
- AI 续写《秋夜》✅ 生成七律续句；仿写"春江·王维风"✅ 生成山水新诗。
- 收藏：未登录 401 ✅ / 收藏 toggle（true→false）✅ / favoritedBy 落库 ✅。
- 用户主页 200（含公开诗）✅ / 不存在 404 ✅；seasons/write?preset/tags/login 全 200 ✅。
- 测试数据已清理；验证消耗少量 AI 额度（2 次续写/仿写调用）。

### 提交
`4c27793`（10 files，+505/-31）。

### 遗留
- P2 仅剩 #20 App 打包（建议不做）。
- 社区化可选增强：关注、评论、同题排行榜。

---

## 会话续记（00:35 · 更新 README）

用户要求更新 README。重写 `README.md`：
- 定位由「私人藏书楼」改为「多用户公开书院」；新增「账号与社区」章节（登录/可见性/真实收藏/用户主页/同题创作/管理员继承）
- AI 能力补充续写/仿写、配图 sharp 自包含；展示体验补充诗词日历/连续朗读/分享页/经典导入/批量打标签
- 新增「环境变量」表（ADMIN_EMAIL 强调、POEM_PASSWORD 标注废弃）
- 目录结构更新（login/u/[userId]/share/api·auth/lib/user 等）
- 路线图更新至当前状态（P0/P1 全部、P2-17/18/19 完成，仅剩 #20）

_（后续会话内容持续追加）_

---

## 会话续记（00:40-00:55 · 社区化增强落地）

用户指示"社区化增强直接做了吧"。三项全部落地（详见 docs/2026-08-09_改动汇总_社区化增强）：
1. **节气同题排行榜**：seasons 页排序切换「最新 / ♥ 最受欢迎」。
2. **关注关系**：follows/followers 双向列表 + GET/POST API + 用户主页关注/粉丝数 + FollowButton（未登录跳登录、不能关注自己）。
3. **评论系统**：PoemComment + GET/POST（≤500 字）/DELETE（本人或诗作者）+ 详情页评论区。

验证（生产模式 12 项全过）：关注 toggle/自关注 400/取消 ✅；评论未登录 401/发表 201/超长 400/公开 GET/C 删他人 403/作者删 ✅；页面全 200 ✅。测试数据已清理。

提交：`3669dfd`（12 files，+671/-11）；另 `4480fb7` 修正 `git add -A` 误提交的用户本地文档（docx，已 gitignore 忽略）。

P2 至此全部完成（17 多用户 / 18 AI 续写 / 19 社区化；20 App 建议不做）。

---

## 会话续记（00:20-00:35 · 模块化整理与 README 更新）

用户要求：整理项目文件、模块化结构、更新 README。

### 重构（commit b1a2e63，11 files，+149/-388）
- 盘点发现 9 个 API 路由各自重复实现 getKv/getPoems/setPoems/getCollections（约 300 行）。
- 新增 `src/lib/store.ts` 统一数据访问层（诗词/藏/评论读写，Redis + 内存回退，单点维护 key 与降级逻辑）。
- 9 个路由删除重复存储代码，统一 import 自 store；comments 路由 getPoem 调用改为 getPoems().find()。
- 净减 239 行；构建通过；生产回归全链路（读/建诗/收藏/评论/详情）200。

### README 更新
- 新增「🏗️ 模块化架构」小节：四层分层（页面/组件/API/服务层）+ 依赖方向 + 数据层收敛/鉴权统一/客户端收敛/按需加载说明。
- 目录结构补 lib/store.ts、components/user、components/comment。

### 验证
- next build 通过；生产回归：GET poems 200 / 建诗 200 / 收藏 200 / 评论 201 / 详情 200；测试数据已清理。
