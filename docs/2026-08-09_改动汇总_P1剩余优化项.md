# 墨韵阁 · 改动汇总（2026-08-09 P1 剩余优化项）

**日期**: 2026-08-09
**会话**: P1 剩余优化项全部落地（日历 / 续读 / 统计 / 批量标签 / 性能与无障碍）
**Git**: `c9574e0`（10 files，+739/-15）
**依据**: docs/2026-08-09_优化分析 的 P1 功能与性能建议（此前的 P1-5/8/11 已于 `a4dcd06` 完成）

---

## 一、改动清单

| # | 优化项 | 文件 | 说明 |
|---|--------|------|------|
| 1 | 诗词日历视图 | `src/app/yuan/sishi-moyuan/calendar/page.tsx`（新增，247 行） | 月历网格：上/下月切换、今日高亮、当日诗数徽标、节气切换标记（getSolarTerm 对比相邻日）；点选日期查看当日诗词列表；骨架屏加载态 |
| 2 | 诗词日历入口 | `src/app/yuan/sishi-moyuan/page.tsx` | Hero 新增「诗词日历」入口（prefetch=false） |
| 3 | AI 朗读延续 | `src/components/poem/TtsSequenceButton.tsx`（新增） | 列表连续朗读：顺序 fetch /api/ai/tts + Audio 播放，播完自动下一首，单首失败跳过，显示 x/y 进度；与 TtsButton 共用音色偏好（poetry.tts.voice）；接入四时墨苑筛选栏（按当前季节筛选结果朗读） |
| 4 | 数据统计增强 | `src/app/stats/page.tsx` | 新增三区块：收藏 Top10（前三名高亮+节气标注）、最长连续写作天数（热力图连续 >0 计算）、节气写作分布（Top8 条形图按节气色） |
| 5 | 批量打标签 | `src/app/tags/page.tsx` | 新增「批量打标签」：进入选择模式 → 全库多选 → 输入标签（逗号分隔，Enter 确认）→ 合并去重逐首 updatePoem（密码保护，失败单首跳过）；加载态改骨架网格 |
| 6 | 字体非阻塞加载 | `src/app/layout.tsx` | Google Fonts 改 `media="print"` + 字符串 `onload="this.media='all'"` 非阻塞（display=swap 已有）；字重 300-700 经核查全部在用未缩减 |
| 7 | 字体变量修复 | `src/app/share/[id]/page.tsx` | 未定义变量 `--font-noto` → `--font-lxgw`（正文实际字体） |
| 8 | API 响应缓存 | `src/lib/api.ts` | getAllPoems 短 TTL(10s) 内存缓存 + 并发请求去重（fetchAllRaw 共享 inflight Promise）；8 处写操作成功后 invalidatePoemsCache() |
| 9 | 路由级分包 | `Navbar.tsx` / `sishi-moyuan/page.tsx` | 低频页链接 `prefetch={false}`（接龙、日历） |
| 10 | 无障碍 | `SearchModal.tsx` / `Navbar.tsx` | 弹窗补 role="dialog"/aria-modal/aria-label，遮罩 aria-hidden，输入框 aria-label；⌘K 按钮 aria-label |

## 二、验证结果

### 构建
- ✅ `next build` 成功，30 路由全部生成，无 TypeScript 错误
- ✅ 新路由 `/yuan/sishi-moyuan/calendar`（First Load 191KB）

### 生产回归（端口 3001）
| 路由 | 结果 |
|------|------|
| `/` | ✅ 200 |
| `/yuan/sishi-moyuan` | ✅ 200 |
| `/yuan/sishi-moyuan/calendar` | ✅ 200（新） |
| `/stats` | ✅ 200（新统计区） |
| `/tags` | ✅ 200（批量打标签） |
| `/trash` | ✅ 200 |
| `/share/nonexistent` | ✅ 404（兜底） |

### 踩坑记录
1. **RSC 序列化错误**：`<link onLoad={fn}>` 触发 "Event handlers cannot be passed to Client Component props"（/trash prerender 失败）。解决方案：改用 `media="print" onload="this.media='all'"`（字符串 HTML 属性，可序列化）。
2. **TS 类型断言**：stats 页 `getSolarTermMeta(key as SolarTermKey)`（termStats 键为 string）。

## 三、P1 收尾状态

- ✅ P1 功能：5 经典导入（a4dcd06）/ 6 日历（本次）/ 7 续读（本次）/ 8 分享页（a4dcd06）/ 9 统计（本次）/ 10 批量（本次）
- ✅ P1 性能体验：11 next/image（a4dcd06）/ 12 字体（本次）/ 13 API 缓存（本次）/ 14 分包（本次）/ 15 骨架屏（本次）/ 16 无障碍（本次）

## 四、遗留事项

- GitHub 推送未成（上轮网络重置）：本地待推 `a4dcd06`、`44de005`、`c9574e0`。
- P0 项未排期：AI 配图对象存储持久化、生产部署凭据就绪、收藏语义澄清、TTS 静态音频补全。
- P2 项需用户决策：多用户/登录、AI 续写、社区化、App 打包。
