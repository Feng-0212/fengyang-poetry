# 墨韵阁 · 改动汇总（2026-08-09 社区化增强：同题排行榜/关注/评论）

**日期**: 2026-08-09
**会话**: 社区化增强（用户指示"直接做了吧"）
**Git**: `3669dfd`（12 files，+671/-11）+ `4480fb7`（修正误提交 docx）
**依据**: 承接 #19 社区化（此前已完成：真实收藏/同题入口/用户主页）

---

## 一、改动清单

### 1. 节气同题排行榜
| 文件 | 说明 |
|------|------|
| `src/app/yuan/sishi-moyuan/seasons/page.tsx` | SolarTermPoemGrid 新增排序切换「最新 / ♥ 最受欢迎」（按 favoriteCount 降序）+ 诗词计数显示 |

### 2. 关注关系
| 文件 | 说明 |
|------|------|
| `src/lib/user.ts` | `follows:{id}` / `followers:{id}` 双向列表 + getFollows/getFollowers/toggleFollow |
| `src/app/api/user/[id]/follow/route.ts`（新增） | GET 查询关注状态+粉丝数（未登录可用）；POST 切换（需登录、不能关注自己 400、目标不存在 404） |
| `src/components/user/FollowButton.tsx`（新增） | 关注按钮：挂载时 GET 同步真实关注态；未登录点击跳登录；自己/未登录显示粉丝数 |
| `src/app/u/[userId]/page.tsx` | 用户主页显示 关注/粉丝 数 + FollowButton |
| `src/lib/api.ts` | toggleFollow / getFollowStatus |

### 3. 评论系统
| 文件 | 说明 |
|------|------|
| `src/types/poem.ts` | 新增 `PoemComment` 类型（id/poemId/userId/userName/content/createdAt） |
| `src/app/api/poem/[id]/comments/route.ts`（新增） | GET 评论列表（公开，私密诗 404）；POST 发表（需登录，1~500 字） |
| `src/app/api/poem/[id]/comments/[cid]/route.ts`（新增） | DELETE 删除（评论者本人或诗作者，否则 403） |
| `src/components/comment/CommentSection.tsx`（新增） | 评论区：列表（头像/@名/时间/内容）、发表框（500 字计数）、删除按钮（权限判定）、骨架屏 |
| `src/app/poem/[id]/page.tsx` | 详情页正文后嵌入评论区 |
| `src/lib/api.ts` | getComments / addComment / deleteComment |

## 二、验证结果（生产模式 3001 端口实测）

| # | 场景 | 结果 |
|---|------|------|
| 1 | B 关注 A | ✅ following:true followers:1 |
| 2 | 未登录 GET 关注态 | ✅ following:false followers:1 |
| 3 | A 关注自己 | ✅ 400 |
| 4 | B 取消关注 | ✅ following:false followers:0 |
| 5 | 未登录发评论 | ✅ 401 |
| 6 | B 发评论 | ✅ 201 |
| 7 | 超 500 字评论 | ✅ 400 |
| 8 | 公开 GET 评论列表 | ✅ 200（内容正确） |
| 9 | C 删 B 的评论（无权限） | ✅ 403 |
| 10 | A（诗作者）删评论 | ✅ 200 |
| 11 | 用户主页 /u/[id] | ✅ 200 |
| 12 | 页面 seasons/tags/login | ✅ 200 |

测试用户、测试诗、评论、follows/followers 已清理。

## 三、说明

- 评论与关注均为轻量 JSON 数组存 Redis（`comments:{poemId}`、`follows:{id}`/`followers:{id}`），单用户规模足够，无需引入关系型存储。
- 删除权限：评论者本人或诗作者（诗作者可管理自己作品下的评论）。
- 关注双向列表保证一致性（toggle 同步写入双方）。

## 四、遗留 / 后续

- 我的关注列表页（聚合关注者的动态流）——当前仅计数与关注按钮，如需可做"关注动态流"。
- 评论回复/点赞、@提醒等社交深度功能未做。
- P2 全部项已完成（17 多用户 / 18 AI 续写 / 19 社区化；20 App 建议不做）。
